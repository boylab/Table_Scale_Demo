<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boylab.tablescale">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Table_Scale_Demo">
        <activity android:name=".calib.activity.CalibWeightActivity"></activity>
        <activity android:name=".calib.activity.CalibParaActivity" />
        <activity android:name=".home.activity.MenuActivity" />
        <activity android:name=".login.activity.UpdateActivity" />
        <activity android:name=".login.activity.LoginActivity" />
        <activity android:name=".login.activity.StartUpActivity" />
        <activity android:name=".home.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>